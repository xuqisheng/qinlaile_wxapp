<!--detail.wxml-->
<view class="main">
  <view class="title">{{data.title}}</view>
  <view class="time">{{data.formattime}}</view>

  <view class="content">
    <rich-text nodes="{{data.content}}" bindtap="tap"></rich-text>
  </view> 
  
  <view class="company">{{data.company_name}}</view>
</view>
